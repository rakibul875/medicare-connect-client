import { Button } from '@heroui/react';
import React from 'react';

const HomePage = () => {
  return (
    <div>
      <Button>Click Me</Button>
      <button className='btn btn-primary'>Native Button</button>
    </div>
  );
};

export default HomePage;